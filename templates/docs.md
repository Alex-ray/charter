---
charter: docs
version: 1
role: Own <project> documentation — reference, guides, examples, and changelog.
scope:
  - README, docs site or docs directory, and inline examples.
  - Changelog and release notes.
fixed_constraints:
  - <your audience decision, e.g. "docs assume a reader who has never used the project">
  - <your voice decision, e.g. "plain declarative sentences; no marketing language in docs">
  - <your structure decision, e.g. "one task per page; reference and tutorial never mix">
  - <your example policy, e.g. "every code example must be runnable as pasted">
open_questions:
  - <something genuinely undecided that agents may discuss>
---

Replace every `<placeholder>` above, then delete this note. Docs constraints
prevent the two classic drifts: voice drift (each edit adds a little more
enthusiasm) and structure drift (reference material growing tutorial prose).
Decide both once, then let the charter hold the line.
